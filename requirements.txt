# VerifEarth - Streamlit Cloud requirements
# torch + transformers are NOT included here:
#   - Cloud mode uses rule-based greenwashing scorer (no model needed)
#   - Removing torch cuts install from ~10min/1GB to ~90sec/150MB
#   - For local dev with full DistilBERT, pip install torch transformers separately

# Core ML (lightweight)
scikit-learn>=1.3.0
numpy>=1.24.0
joblib>=1.3.0

# NLP / PDF
pypdf>=3.17.0
pdfplumber>=0.10.0

# Geospatial
rasterio>=1.3.8
pystac-client>=0.7.5
planetary-computer>=1.0.0
requests>=2.31.0
pillow>=10.1.0

# Visualization
matplotlib>=3.8.0
plotly>=5.18.0

# Demo UI
streamlit>=1.29.0

# Utilities
python-dotenv>=1.0.0
